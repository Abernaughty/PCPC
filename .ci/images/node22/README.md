pcpc-ci-node22 image

Purpose
- Provides a deterministic Node 22 toolchain for frontend/backend/APIM validation (Node/NPM, Git, Spectral CLI).

Notes
- Dockerfile will be added in Stage 2 using versions from ../../versions.yaml.
- Playwright/browser support is intentionally omitted at this stage.

