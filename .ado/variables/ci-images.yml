# CI Container Image References (populate digests in later stages)
# Usage example in jobs:
#   resources:
#     containers:
#       - container: tf
#         image: $(CI_IMAGE_TERRAFORM)
#         endpoint: $(ACR_SERVICE_CONNECTION)
#   jobs:
#     - job: ValidateInfrastructure
#       container: tf

variables:
  ACR_NAME: maberdevcontainerregistry-ccedhvhwfndwetdp
  ACR_FQDN: $(ACR_NAME).azurecr.io

  # Populate these with actual digests after Stage 3 build/publish.
  CI_IMAGE_TERRAFORM: $(ACR_FQDN)/pcpc-ci-terraform@sha256:TO_BE_FILLED
  CI_IMAGE_NODE22:    $(ACR_FQDN)/pcpc-ci-node22@sha256:TO_BE_FILLED

  # Name of the ACR service connection in Azure DevOps
  ACR_SERVICE_CONNECTION: pcpc-acr-service-connection

